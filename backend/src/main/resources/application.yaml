spring:
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: backend

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: 8080

logging:
  level:
    root: INFO